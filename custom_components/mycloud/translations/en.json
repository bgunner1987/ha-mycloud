{
    "config": {
        "step": {
            "user": {
                "description": "Configure access to the WD My Cloud API. Optional sleep-aware polling checks every disk over SSH and performs full API polls only for a confirmed wake phase or when the API interval is due. Power checks default to 10 seconds. The minimum API interval is {minimum_interval} seconds.",
                "data": {
                    "Host": "Host",
                    "Username": "Username",
                    "Password": "Password",
                    "Version": "My Cloud OS version",
                    "update_interval": "API update interval (seconds)",
                    "power_probe_interval": "Disk power probe interval (seconds)",
                    "sleep_aware_enabled": "Enable sleep-aware polling",
                    "ssh_port": "SSH port",
                    "ssh_username": "SSH username",
                    "ssh_password": "SSH password",
                    "drive_devices": "Drive devices"
                },
                "data_description": {
                    "update_interval": "Legacy polling interval, and the minimum spacing for periodic full API snapshots in sleep-aware mode. Does not control disk power checks.",
                    "power_probe_interval": "Non-waking SSH/hdparm checks in sleep-aware mode. Default and minimum 10 seconds.",
                    "sleep_aware_enabled": "Requires SSH and /usr/bin/hdparm on the NAS. Leave disabled for legacy behavior. Unknown states block WD API polling but retain each disk's last confirmed Sleeping state.",
                    "ssh_password": "Stored by Home Assistant. Use a dedicated credential where possible and restrict access to Home Assistant backups.",
                    "drive_devices": "Comma-separated whole-disk paths, for example /dev/sda,/dev/sdc."
                }
            }
        },
        "error": {
            "invalid_drive_devices": "Enter unique whole-disk paths separated by commas, for example /dev/sda,/dev/sdc. Partitions and shell commands are not allowed.",
            "auth": "Username/Password is wrong.",
            "connection": "Unable to connect to the server.",
            "unknown": "Unknown error occurred."
        }
    },
    "options": {
        "error": {
            "invalid_drive_devices": "Enter unique whole-disk paths separated by commas, for example /dev/sda,/dev/sdc. Partitions and shell commands are not allowed."
        },
        "step": {
            "init": {
                "title": "Polling options",
                "description": "Sleep-aware polling checks every configured drive with hdparm and performs full API polls only for a confirmed wake phase or when the API interval is due. Power checks default to 10 seconds. The minimum API interval is {minimum_interval} seconds.",
                "data": {
                    "update_interval": "API update interval (seconds)",
                    "power_probe_interval": "Disk power probe interval (seconds)",
                    "sleep_aware_enabled": "Enable sleep-aware polling",
                    "ssh_port": "SSH port",
                    "ssh_username": "SSH username",
                    "ssh_password": "SSH password",
                    "drive_devices": "Drive devices"
                },
                "data_description": {
                    "update_interval": "Legacy polling interval, and the minimum spacing for periodic full API snapshots in sleep-aware mode. Does not control disk power checks.",
                    "power_probe_interval": "Non-waking SSH/hdparm checks in sleep-aware mode. Default and minimum 10 seconds.",
                    "sleep_aware_enabled": "Requires SSH and /usr/bin/hdparm on the NAS. Unknown states block WD API polling but retain each disk's last confirmed Sleeping state.",
                    "ssh_password": "Stored by Home Assistant. Use a dedicated credential where possible and restrict access to Home Assistant backups.",
                    "drive_devices": "Comma-separated whole-disk paths, for example /dev/sda,/dev/sdc."
                }
            }
        }
    }
}
